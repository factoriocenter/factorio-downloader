# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts must always use LF, even on Windows checkouts, or they break on
# the Linux server (CRLF in the shebang/commands makes bash fail).
*.sh text eol=lf
